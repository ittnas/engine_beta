#include "bounding_geometry.hpp"

BoundingGeometry::BoundingGeometry(CollisionObject * collision_object, GLuint position_array_index) : target_object(target_object), position_array_index(position_array_index) {
  recalculate_mesh(); // Calculates the mesh.
}

void BoundingGeometry::recalculate_mesh() {
  std::vector<glm::vec4> mesh;
  for(auto child : target_object->get_children()) {
    if(child->get_shape != 0) {
      
    }
  }
}


