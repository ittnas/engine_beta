#ifndef FORCE_HPP
#define FORCE_HPP
#define GLM_FORCE_RADIANS
#include <GL/glew.h>
#include <glm/glm.hpp>
class Force {
public:
  GLfloat get_strength(glm::vec3 position);
private:
  virtual GLfloat get_strength_sub_class(glm::vec3 position);
};

class ConstantForce : public Force {
  
};

#endif
