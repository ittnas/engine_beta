#ifndef GLOBAL_CONTEXT
#define GLOBAL_CONTEXT

#include "matrix_stack.hpp"

namespace GlobalContext {
  static MatrixStack * model_stack = new MatrixStack;
  static MatrixStack * projection_stack = new MatrixStack;
  
}

#endif
