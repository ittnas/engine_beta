#ifndef AABB_HPP
#define AABB_HPP

#include <GL/glew.h>
#include <glm/glm.hpp>

class AABB {
private:
  glm::vec4 vertices[8];
  
public:
  AABB(GLfloat * min, GLfloat * max);
  AABB(glm::vec4 * vertices);
  ~AABB() {}

  glm::vec4 * get_vertices() {return vertices;}
  
};

#endif
