#ifndef LIGHTABLE_HPP
#define LIGHTABLE_HPP

#include "lightning_buffer.hpp"

class Lightable : virtual public Object {
private:
  LightningBuffer * lightning_buffer;
public:
  Lightable();
};

#endif
